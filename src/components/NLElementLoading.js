import React from 'react';
import {Animated, StyleSheet} from 'react-native';

import AppStyles from '../config/Styles';

export default class NLElementLoading extends React.Component {
  render() {
    return (
      <Animated.View>
        <Text></Text>
      </Animated.View>
    )
  }
}

const styles = StyleSheet.create({
  
});
